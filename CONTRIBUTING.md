# 기여 지침

새 UI·사용 예·의미 판단 지시문과 시험은 한국어를 기준으로 작성합니다. 기존 영어 단위 시험은 원본 동작 회귀 근거로 유지하며 새 기능의 한국어 품질 증거로 혼합하지 않습니다.

README의 환경 준비 후 변경 영역에 필요한 테스트와 타입 검사·빌드를 실행하세요. 네트워크는 모의 처리하며 실제 API 키나 개인 검색어가 없어도 통과해야 합니다. 키·가족 음성·전사·실제 제공자 응답·.dev.vars·.env·생성 빌드는 커밋하지 않습니다.

원문을 바꿔 고유명사·부정·날짜를 잃지 마세요. 의미 판단은 Jev, 공급자 선택 정책·기한·취소·URL 검증·권한은 코드가 담당합니다. 결과 부족과 서비스 실패를 구분하고 미평가를 관련성 0의 확정 판정으로 표시하지 않습니다.

원본 MIT LICENSE와 출처를 보존하세요. 원본 코드를 변경할 때는 독립 파생본임을 명확히 합니다. `worker-configuration.d.ts`는 `pnpm cf-typegen`으로 생성합니다.
