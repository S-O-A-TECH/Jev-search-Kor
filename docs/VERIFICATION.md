# 검증 기록

기준: 2026-09-19 · 원본: 8b34965084be05bc2be8dffeb9c2d2887d7e9a54

## 수행 결과

- Node 22.23.2 / pnpm 10.8.0, 원본 잠금 파일 기반 설치 (`--frozen-lockfile --ignore-scripts`).
- `pnpm test`: 16개 파일, **136개 단위·모의 통합 테스트 통과**. 원본의 일반 회귀 시험도 포함하며 모두 한국어 의미 정확도 시험이라는 뜻은 아닙니다.
- 한국어 추가 시험: 고유명사·단답·부정·날짜 원문 보존, Tavily/네이버 요청 계약, 병렬 시작, URL 중복과 출처 보존, 단일 Jev 근거 배치.
- 오류 시험: HTTP 401/429/503, 미설정, 잘못된 응답, 4초 기한, 사용자 취소, Jev 판정 누락, 원격 인증·큰 본문 차단, NDJSON 소비 취소.
- `pnpm build`: 클라이언트/Worker 번들 및 TypeScript 검사 통과.
- `wrangler deploy --dry-run`: 패키지 점검 통과. **실제 배포 아님**.
- 로컬 실제 브라우저: 한국어 제목·입력·예시·두 공급자 표시 확인. 예시 클릭 후 키 미설정 상태가 '검색 실패 / 검색 서버 설정을 확인해 주세요.'로 종료되는 것 확인.
- 런타임 고정 버전은 2026-09-19 호환 날짜를 지원하지 않아 원본의 지원 날짜 `2026-07-17`로 수정 후 로컬 기동 확인.
- `git diff --check`와 공개 파일의 키 패턴 점검 수행. 키·.dev.vars·빌드 산출물·실사용 기록은 게시 대상이 아닙니다.

## 미수행

- 실제 Tavily·네이버·Jev API 호출 및 인증 확인.
- 실제 한국어 검색 관련성/근거 판정 정확도, 비용·p50/p95·망 지연 측정.
- DeepSeek 답변, Python RAG 라우터, STT/TTS, Pi 배포·재시작·음향·가족 실사용.

현재 테스트는 코드 계약을 확인합니다. 모의 답변은 모델의 실제 한국어 성능 증거가 아니며 이 저장소의 완벽성·스피커 개선을 보증하지 않습니다.
