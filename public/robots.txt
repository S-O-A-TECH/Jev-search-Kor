# Result pages send noindex in HTML. Do not Disallow /search, or crawlers
# cannot see that directive on URLs they already discovered.

User-agent: *
Allow: /
Disallow: /api/

Sitemap: http://localhost:3030/sitemap.xml
